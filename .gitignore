# Dependencias
node_modules/
.pnpm-store/

# Artefactos de build
dist/
out/
*.tsbuildinfo

# Datos del taller: base SQLite, fotos de equipos de clientes y comprobantes en
# PDF. En la Pi viven en un volumen de Docker, pero si alguna vez se corre el
# servidor fuera del contenedor aparecen acá. Es información personal de gente
# real y este repositorio es público.
datos/

# Entorno / secretos
.env
.env.*
!.env.example

# Sitios anteriores: se conservan en disco como referencia, pero no se
# versionan ni se publican.
legacy/

# Sistema
.DS_Store
Thumbs.db
*.log
npm-debug.log*
pnpm-debug.log*

# Editores
.vscode/*
!.vscode/extensions.json
.idea/
